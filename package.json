{
  "name": "my-dashboard-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:web": "pnpm --filter my-dashboard-web dev",
    "build:web": "pnpm --filter my-dashboard-web build",
    "preview:web": "pnpm --filter my-dashboard-web preview",
    "dev:api": "pnpm --filter my-dashboard-api dev",
    "build:api": "pnpm --filter my-dashboard-api build",
    "lint:api": "pnpm --filter my-dashboard-api lint",
    "build": "pnpm build:web && pnpm build:api"
  }
}
